# 🎬 Modern Lipsync - Полное руководство по использованию

## ✅ Проект успешно создан!

Современный аналог Wav2Lip с поддержкой PyTorch 2.8.0+ и torchaudio 2.8.0+

---

## 📍 Расположение проекта

```bash
/workspace/modern-lipsync/
```

---

## 🎯 Что было сделано

### ✅ Созданные компоненты:

1. **Модели Wav2Lip** (обновлены для PyTorch 2.8.0)
   - `models/wav2lip.py` - архитектура модели
   - `models/conv.py` - сверточные блоки
   - 36.3M параметров

2. **Обработка аудио** (с torchaudio 2.8.0)
   - `utils/audio.py` - ModernAudioProcessor
   - GPU-ускоренная обработка
   - Mel-спектрограммы на GPU

3. **Детекция лиц** (из camenduru/Wav2Lip)
   - `face_detection/` - полный модуль S3FD
   - Включены предобученные веса

4. **Скрипт инференса**
   - `inference.py` - модернизированный главный скрипт
   - Поддержка видео и изображений
   - Батч-обработка

5. **Документация**
   - `README.md` - полная документация
   - `SUMMARY.md` - краткое резюме
   - `ARCHITECTURE.md` - описание архитектуры

6. **Утилиты**
   - `test_components.py` - тестирование
   - `setup.sh` - скрипт установки
   - `copy_weights.sh` - копирование весов

---

## 🚀 Как использовать

### Шаг 1: Проверка компонентов

```bash
cd /workspace/modern-lipsync
python test_components.py
```

**Ожидаемый результат:**
```
✅ All tests passed!
- PyTorch 2.8.0+cu128
- CUDA 12.8
- Model parameters: 36,298,035
```

### Шаг 2: Подготовка весов

Веса моделей уже доступны в `/workspace/`:
- `Wav2Lip-SD-GAN.pt` - модель с GAN
- `Wav2Lip-SD-NOGAN.pt` - базовая модель

**Опционально:** скопируйте веса в проект:
```bash
cd /workspace/modern-lipsync
./copy_weights.sh
```

### Шаг 3: Запуск инференса

#### Вариант 1: Видео + Аудио

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-GAN.pt \
  --face input_video.mp4 \
  --audio speech.wav \
  --outfile results/output.mp4
```

#### Вариант 2: Изображение + Аудио

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-NOGAN.pt \
  --face portrait.jpg \
  --audio voice.wav \
  --static \
  --fps 25 \
  --outfile results/talking_portrait.mp4
```

#### Вариант 3: С настройками качества

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-GAN.pt \
  --face hd_video.mp4 \
  --audio audio.wav \
  --pads 0 20 0 0 \
  --resize_factor 1 \
  --wav2lip_batch_size 128 \
  --face_det_batch_size 16 \
  --outfile results/hq_output.mp4
```

---

## ⚙️ Важные параметры

### Основные

| Параметр | Описание | Пример |
|----------|----------|--------|
| `--checkpoint_path` | Путь к весам модели | `/workspace/Wav2Lip-SD-GAN.pt` |
| `--face` | Видео или изображение | `video.mp4` или `photo.jpg` |
| `--audio` | Аудио файл | `speech.wav` |
| `--outfile` | Выходной файл | `results/output.mp4` |

### Настройка качества

| Параметр | Рекомендуемое | Описание |
|----------|---------------|----------|
| `--pads` | `0 20 0 0` | Отступы (верх низ лево право) |
| `--resize_factor` | `1` или `2` | Уменьшение разрешения |
| `--wav2lip_batch_size` | `128` | Размер батча для модели |
| `--face_det_batch_size` | `16` | Размер батча для детекции |

### Специальные флаги

| Флаг | Когда использовать |
|------|-------------------|
| `--static` | Для статических изображений |
| `--nosmooth` | Если рот смещен/артефакты |
| `--rotate` | Если видео повернуто на 90° |

---

## 💡 Советы для лучших результатов

### 1. Настройка отступов (pads)

**Проблема:** Подбородок обрезан или рот неточно синхронизирован

**Решение:**
```bash
--pads 0 20 0 0  # Увеличить нижний отступ
--pads 10 20 10 10  # Увеличить все стороны
```

### 2. Проблемы с детекцией

**Проблема:** Лицо не детектируется или детекция неточная

**Решение:**
```bash
--nosmooth  # Отключить сглаживание
--resize_factor 2  # Уменьшить разрешение
--box 100 300 50 250  # Указать bbox вручную
```

### 3. Выбор модели

**Wav2Lip-SD-GAN.pt:**
- ✅ Лучшее визуальное качество
- ⚠️ Может быть менее точная синхронизация

**Wav2Lip-SD-NOGAN.pt:**
- ✅ Более точная синхронизация
- ⚠️ Может требовать настройки параметров

### 4. Оптимизация производительности

**Для мощных GPU (RTX 5090/4090):**
```bash
--wav2lip_batch_size 128
--face_det_batch_size 32
```

**Для средних GPU (RTX 3090/3080):**
```bash
--wav2lip_batch_size 64
--face_det_batch_size 16
```

**Для слабых GPU или CPU:**
```bash
--wav2lip_batch_size 16
--face_det_batch_size 4
--device cpu  # Если GPU недостаточно памяти
```

---

## 🐛 Решение проблем

### Ошибка: "Face not detected"

```bash
# Решение 1: Увеличить отступы
--pads 10 20 10 10

# Решение 2: Уменьшить разрешение
--resize_factor 2

# Решение 3: Указать bbox вручную
--box 100 300 50 250
```

Файл с проблемным кадром сохраняется в `temp/faulty_frame.jpg`

### Ошибка: "CUDA Out of Memory"

```bash
# Решение 1: Уменьшить размер батча
--wav2lip_batch_size 32
--face_det_batch_size 8

# Решение 2: Использовать CPU
--device cpu

# Решение 3: Уменьшить разрешение
--resize_factor 2
```

### Ошибка: "Mel contains nan"

```bash
# Проблема с TTS аудио
# Решение: добавьте шум к аудио
ffmpeg -i input.wav -af "volume=1.01" output.wav
```

### Проблемы с аудио форматом

```bash
# Конвертируйте в WAV 16kHz
ffmpeg -i input.mp3 -ar 16000 -ac 1 output.wav

# Используйте этот файл
--audio output.wav
```

---

## 📊 Тестирование производительности

### Проверка GPU

```bash
nvidia-smi
```

### Проверка компонентов

```bash
cd /workspace/modern-lipsync
python test_components.py
```

### Проверка зависимостей

```bash
pip list | grep -E "torch|numpy|opencv"
```

**Ожидаемые версии:**
- torch: 2.8.0+cu128
- torchaudio: 2.8.0+cu128
- numpy: 2.1.2
- opencv-python: 4.10.0+

---

## 📁 Структура выходных файлов

```
modern-lipsync/
├── results/
│   └── output.mp4          # Финальное видео
├── temp/
│   ├── temp.wav            # Извлеченное аудио
│   ├── result.avi          # Промежуточное видео
│   └── faulty_frame.jpg    # Кадр с ошибкой (если есть)
└── checkpoints/
    ├── Wav2Lip-SD-GAN.pt   # Опционально
    └── Wav2Lip-SD-NOGAN.pt # Опционально
```

---

## 🎓 Примеры команд

### Пример 1: Базовое использование

```bash
cd /workspace/modern-lipsync

python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-GAN.pt \
  --face person.mp4 \
  --audio speech.wav
```

**Результат:** `results/result_voice.mp4`

### Пример 2: Высокое качество

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-GAN.pt \
  --face hd_video.mp4 \
  --audio audio.wav \
  --pads 0 15 0 0 \
  --resize_factor 1 \
  --wav2lip_batch_size 64 \
  --outfile results/hq_result.mp4
```

### Пример 3: Статический портрет

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-NOGAN.pt \
  --face portrait.jpg \
  --audio narration.wav \
  --static \
  --fps 25 \
  --pads 0 10 0 0 \
  --outfile results/talking_portrait.mp4
```

### Пример 4: Максимальная производительность

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-GAN.pt \
  --face video.mp4 \
  --audio audio.wav \
  --wav2lip_batch_size 128 \
  --face_det_batch_size 32 \
  --resize_factor 1 \
  --outfile results/fast_output.mp4
```

---

## 🔗 Полезные ссылки

### Документация проекта:
- `README.md` - Полная документация
- `SUMMARY.md` - Краткое резюме
- `ARCHITECTURE.md` - Описание архитектуры
- `USAGE_GUIDE.md` - Этот файл

### Внешние ресурсы:
- [Original Wav2Lip Paper](http://arxiv.org/abs/2008.10010)
- [Wav2Lip GitHub](https://github.com/Rudrabha/Wav2Lip)
- [camenduru/Wav2Lip HuggingFace](https://huggingface.co/camenduru/Wav2Lip)
- [PyTorch Documentation](https://pytorch.org/docs/)
- [torchaudio Documentation](https://pytorch.org/audio/)

---

## ✅ Чеклист перед запуском

- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Проверена работа GPU (`nvidia-smi`)
- [ ] Веса моделей доступны (`/workspace/Wav2Lip-SD-*.pt`)
- [ ] Установлен ffmpeg (`ffmpeg -version`)
- [ ] Тесты пройдены (`python test_components.py`)
- [ ] Входные файлы готовы (видео + аудио)

---

## 🎉 Готово!

Ваш проект Modern Lipsync полностью настроен и готов к использованию!

**Следующие шаги:**
1. Подготовьте входные файлы (видео и аудио)
2. Выберите подходящую модель (GAN или NOGAN)
3. Запустите inference с нужными параметрами
4. Наслаждайтесь результатом! 🚀

---

**Modern Lipsync** - Профессиональный lip-sync для 2025 года 🎬
