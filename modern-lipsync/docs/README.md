# Modern Lipsync

> Современная реализация Wav2Lip с поддержкой PyTorch 2.8.0+ и torchaudio 2.8.0+

Это модернизированная версия [Wav2Lip](https://github.com/Rudrabha/Wav2Lip), обновленная для работы с последними версиями PyTorch, torchaudio и других библиотек.

## 🚀 Особенности

- ✅ **PyTorch 2.8.0+** - Полная совместимость с новейшими версиями
- ✅ **torchaudio 2.8.0+** - Современная обработка аудио без librosa
- ✅ **Оптимизированный код** - Улучшенная производительность и читаемость
- ✅ **Поддержка CUDA 12.8** - Максимальное использование GPU
- ✅ **Совместимость с весами** - Работает с Wav2Lip-SD-GAN.pt и Wav2Lip-SD-NOGAN.pt

## 📋 Требования

### Системные требования

- Python 3.10+
- CUDA 12.8+ (для GPU)
- ffmpeg (для обработки видео и аудио)

### Установка ffmpeg

```bash
# Ubuntu/Debian
sudo apt-get install ffmpeg

# macOS
brew install ffmpeg

# Windows
# Скачайте с https://ffmpeg.org/download.html
```

## 🔧 Установка

### 1. Клонирование репозитория

```bash
cd /workspace/modern-lipsync
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Подготовка весов модели

Разместите файлы весов в корне workspace:
- `Wav2Lip-SD-GAN.pt` - модель с GAN для лучшего визуального качества
- `Wav2Lip-SD-NOGAN.pt` - базовая модель без GAN

### 4. Загрузка детектора лиц

Детектор лиц (s3fd.pth) уже включен из репозитория camenduru/Wav2Lip.

## 🎬 Использование

### Базовое использование

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-GAN.pt \
  --face input_video.mp4 \
  --audio input_audio.wav
```

### Полный пример с параметрами

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-NOGAN.pt \
  --face input_video.mp4 \
  --audio speech.wav \
  --outfile results/output.mp4 \
  --pads 0 10 0 0 \
  --resize_factor 1 \
  --wav2lip_batch_size 128
```

### Работа со статическим изображением

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-GAN.pt \
  --face portrait.jpg \
  --audio speech.wav \
  --static \
  --fps 25
```

## ⚙️ Параметры командной строки

### Основные параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--checkpoint_path` | Путь к весам модели (обязательно) | - |
| `--face` | Путь к видео/изображению с лицом (обязательно) | - |
| `--audio` | Путь к аудио/видео файлу (обязательно) | - |
| `--outfile` | Путь для сохранения результата | `results/result_voice.mp4` |

### Параметры обработки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--static` | Использовать только первый кадр | `False` |
| `--fps` | FPS для статического изображения | `25.0` |
| `--pads` | Отступы для детекции лица (верх низ лево право) | `0 10 0 0` |
| `--resize_factor` | Коэффициент уменьшения разрешения | `1` |
| `--crop` | Обрезка видео (верх низ лево право) | `0 -1 0 -1` |
| `--box` | Фиксированная область лица | `-1 -1 -1 -1` |
| `--rotate` | Повернуть видео на 90° по часовой | `False` |
| `--nosmooth` | Отключить сглаживание детекции | `False` |

### Параметры производительности

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--face_det_batch_size` | Размер батча для детекции лиц | `16` |
| `--wav2lip_batch_size` | Размер батча для Wav2Lip | `128` |
| `--device` | Устройство (cuda/cpu) | `cuda` (если доступен) |

## 💡 Советы для лучших результатов

### 1. Настройка отступов (pads)

Увеличьте нижний отступ для включения подбородка:

```bash
--pads 0 20 0 0  # Увеличен нижний отступ до 20
```

### 2. Проблемы с детекцией

Если рот смещен или есть артефакты, отключите сглаживание:

```bash
--nosmooth
```

### 3. Оптимизация разрешения

Для лучших результатов попробуйте снизить разрешение:

```bash
--resize_factor 2  # Уменьшить разрешение в 2 раза
```

### 4. Выбор модели

- **Wav2Lip-SD-GAN.pt**: Лучшее визуальное качество, может быть менее точной синхронизация
- **Wav2Lip-SD-NOGAN.pt**: Более точная синхронизация, но может требовать настройки параметров

## 🏗️ Структура проекта

```
modern-lipsync/
├── models/                    # Модели PyTorch
│   ├── __init__.py
│   ├── conv.py               # Сверточные блоки
│   └── wav2lip.py            # Архитектура Wav2Lip
├── face_detection/            # Детекция лиц
│   ├── detection/            # Модули S3FD
│   ├── api.py
│   ├── models.py
│   └── utils.py
├── utils/                     # Утилиты
│   └── audio.py              # Обработка аудио с torchaudio
├── checkpoints/               # Папка для весов (опционально)
├── results/                   # Результаты inference
├── temp/                      # Временные файлы
├── hparams.py                # Гиперпараметры
├── inference.py              # Главный скрипт
├── requirements.txt          # Зависимости
└── README.md                 # Документация
```

## 🔬 Технические детали

### Обработка аудио

Модуль `utils/audio.py` использует torchaudio 2.8.0 для:
- Загрузки аудио файлов
- Преобразования Mel-спектрограмм
- Ресемплинга
- Preemphasis фильтрации

Все операции выполняются на GPU (если доступен) для максимальной скорости.

### Архитектура модели

Wav2Lip состоит из:
- **Face Encoder**: Кодирует изображение лица
- **Audio Encoder**: Кодирует Mel-спектрограмму
- **Face Decoder**: Генерирует синхронизированный рот

### Детекция лиц

Используется детектор S3FD (Single Shot Scale-invariant Face Detector) для:
- Обнаружения лиц в кадрах
- Определения области рта
- Сглаживания детекций во времени

## 📊 Производительность

### Рекомендуемые настройки GPU

- **RTX 3090/4090**: `--wav2lip_batch_size 128`
- **RTX 3080**: `--wav2lip_batch_size 64`
- **RTX 3060**: `--wav2lip_batch_size 32`
- **CPU**: `--wav2lip_batch_size 8 --device cpu`

### Использование памяти

- Модель: ~500 MB
- Детектор лиц: ~90 MB
- Батч inference: зависит от размера батча

## 🐛 Устранение неполадок

### Ошибка: Face not detected

```bash
# Попробуйте увеличить отступы
--pads 10 20 10 10

# Или уменьшите разрешение
--resize_factor 2

# Или используйте фиксированную область
--box 100 300 50 250
```

### Ошибка: CUDA Out of Memory

```bash
# Уменьшите размер батча
--wav2lip_batch_size 32
--face_det_batch_size 8

# Или используйте CPU
--device cpu
```

### Проблемы с аудио

```bash
# Убедитесь, что ffmpeg установлен
ffmpeg -version

# Конвертируйте аудио в WAV 16kHz
ffmpeg -i input.mp3 -ar 16000 output.wav
```

## 📝 Примеры

### Пример 1: Базовая синхронизация

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-GAN.pt \
  --face person.mp4 \
  --audio speech.wav \
  --outfile results/synced_video.mp4
```

### Пример 2: Высокое качество

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-GAN.pt \
  --face hd_video.mp4 \
  --audio audio.wav \
  --resize_factor 1 \
  --pads 0 15 0 0 \
  --wav2lip_batch_size 64 \
  --outfile results/hq_output.mp4
```

### Пример 3: Статический портрет

```bash
python inference.py \
  --checkpoint_path /workspace/Wav2Lip-SD-NOGAN.pt \
  --face portrait.jpg \
  --audio narration.wav \
  --static \
  --fps 25 \
  --pads 0 10 0 0 \
  --outfile results/talking_portrait.mp4
```

## 🤝 Благодарности

- Оригинальный [Wav2Lip](https://github.com/Rudrabha/Wav2Lip) от Rudrabha Mukhopadhyay
- [camenduru/Wav2Lip](https://huggingface.co/camenduru/Wav2Lip) за предобученные веса
- PyTorch и torchaudio команды за отличные библиотеки

## 📄 Лицензия

Этот проект предназначен только для исследовательских/личных целей. Для коммерческого использования свяжитесь с оригинальными авторами Wav2Lip.

## 🔗 Ссылки

- [Wav2Lip Paper](http://arxiv.org/abs/2008.10010)
- [Original Repository](https://github.com/Rudrabha/Wav2Lip)
- [HuggingFace Model](https://huggingface.co/camenduru/Wav2Lip)
- [PyTorch Documentation](https://pytorch.org/docs/)
- [torchaudio Documentation](https://pytorch.org/audio/)

---

**Modern Lipsync** - Обновленная версия Wav2Lip для PyTorch 2.8.0+ 🎬
