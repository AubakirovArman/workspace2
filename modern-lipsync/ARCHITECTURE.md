# 🏗️ Modern Lipsync - Архитектура проекта

## 📐 Общая структура

```
modern-lipsync/
│
├── 🎯 Основные скрипты
│   ├── inference.py          # Главный скрипт инференса
│   ├── hparams.py           # Гиперпараметры
│   └── test_components.py   # Тестирование компонентов
│
├── 🤖 Модели (models/)
│   ├── __init__.py
│   ├── wav2lip.py           # Архитектура Wav2Lip
│   │   ├── Wav2Lip          # Основная модель
│   │   └── Wav2Lip_disc_qual # Дискриминатор качества
│   └── conv.py              # Сверточные блоки
│       ├── Conv2d
│       ├── Conv2dTranspose
│       └── nonorm_Conv2d
│
├── 👤 Детекция лиц (face_detection/)
│   ├── api.py               # API для детекции
│   ├── models.py            # Модели детекции
│   ├── utils.py             # Утилиты
│   └── detection/
│       ├── core.py
│       └── sfd/             # S3FD детектор
│           ├── s3fd.pth     # Веса детектора
│           ├── net_s3fd.py
│           ├── sfd_detector.py
│           ├── detect.py
│           └── bbox.py
│
├── 🔧 Утилиты (utils/)
│   ├── __init__.py
│   └── audio.py             # Обработка аудио
│       ├── ModernAudioProcessor
│       ├── AudioConfig
│       └── Функции совместимости
│
├── 📁 Данные
│   ├── checkpoints/         # Веса моделей
│   ├── results/            # Результаты
│   └── temp/               # Временные файлы
│
├── 📜 Скрипты установки
│   ├── setup.sh            # Установка зависимостей
│   └── copy_weights.sh     # Копирование весов
│
└── 📚 Документация
    ├── README.md           # Полная документация
    ├── README_SHORT.md     # Краткое руководство
    ├── SUMMARY.md          # Резюме проекта
    ├── ARCHITECTURE.md     # Этот файл
    ├── requirements.txt    # Зависимости
    └── .gitignore         # Игнорируемые файлы
```

## 🔄 Поток данных

### 1. Входные данные
```
Видео/Изображение + Аудио
         ↓
  inference.py
```

### 2. Обработка аудио
```
Аудио файл (.wav, .mp3, .mp4)
         ↓
ModernAudioProcessor
         ↓
torchaudio.load()
         ↓
Resampling (16kHz)
         ↓
Preemphasis фильтр
         ↓
MelSpectrogram transform
         ↓
AmplitudeToDB
         ↓
Нормализация
         ↓
Mel-спектрограмма (80 x T)
```

### 3. Обработка видео
```
Видео/Изображение
         ↓
cv2.VideoCapture / cv2.imread
         ↓
Опционально: resize, rotate, crop
         ↓
Список кадров (frames)
         ↓
FaceAlignment (S3FD)
         ↓
Детекция лица + bbox
         ↓
Опционально: сглаживание bbox
         ↓
Обрезка области лица
         ↓
Resize до 96x96
```

### 4. Инференс модели
```
Face frames (96x96) + Mel chunks (80x16)
         ↓
Масштабирование лица (нижняя половина = 0)
         ↓
Concatenate [masked_face, face]
         ↓
Face Encoder (CNN)
    ├── Conv2d блоки
    ├── Residual connections
    └── Skip connections
         ↓
Audio Encoder (CNN)
    ├── Conv2d блоки
    └── Global encoding
         ↓
Concatenate [audio_embedding, face_features]
         ↓
Face Decoder (Transposed CNN)
    ├── Conv2dTranspose блоки
    ├── Skip connections от encoder
    └── Residual blocks
         ↓
Output block
    ├── Conv2d
    └── Sigmoid
         ↓
Сгенерированный рот (96x96x3)
```

### 5. Постобработка
```
Предсказание модели (96x96)
         ↓
Resize к исходному размеру bbox
         ↓
Вставка в оригинальный кадр
         ↓
cv2.VideoWriter
         ↓
Сохранение видео (.avi)
         ↓
ffmpeg: объединение с аудио
         ↓
Финальное видео (.mp4)
```

## 🧩 Ключевые компоненты

### ModernLipsyncInference (inference.py)

**Основной класс для инференса**

```python
class ModernLipsyncInference:
    def __init__(args)              # Инициализация
    def load_video_frames()         # Загрузка видео
    def prepare_audio()             # Обработка аудио
    def face_detect(images)         # Детекция лиц
    def datagen(frames, mels)       # Генератор батчей
    def load_model(checkpoint)      # Загрузка модели
    def run()                       # Запуск инференса
```

### Wav2Lip (models/wav2lip.py)

**Архитектура модели**

```python
class Wav2Lip(nn.Module):
    - face_encoder_blocks    # 7 блоков (6→512 каналов)
    - audio_encoder          # 13 слоев (1→512 каналов)
    - face_decoder_blocks    # 7 блоков (512→64 каналов)
    - output_block          # 3 канала RGB + Sigmoid
    
    forward(audio, face)    # Прямой проход
```

### ModernAudioProcessor (utils/audio.py)

**Обработка аудио с torchaudio**

```python
class ModernAudioProcessor:
    - mel_transform          # MelSpectrogram
    - amplitude_to_db        # Преобразование в dB
    
    load_audio(path)         # Загрузка аудио
    save_audio(wav, path)    # Сохранение аудио
    preemphasis(wav)         # Preemphasis фильтр
    melspectrogram(wav)      # Mel-спектрограмма
    extract_audio_features() # Извлечение признаков
```

### FaceAlignment (face_detection/api.py)

**Детекция лиц**

```python
class FaceAlignment:
    - face_detector          # SFDDetector (S3FD)
    
    get_detections_for_batch() # Детекция в батче
```

## 📊 Параметры моделей

### Wav2Lip Model

| Компонент | Параметры | Описание |
|-----------|-----------|----------|
| Face Encoder | ~8.5M | 7 блоков CNN |
| Audio Encoder | ~4.2M | 13 слоев CNN |
| Face Decoder | ~23.5M | 7 блоков Transposed CNN |
| Output Block | ~0.1M | Финальная свертка |
| **Всего** | **36.3M** | Общие параметры |

### Audio Config

| Параметр | Значение | Описание |
|----------|----------|----------|
| sample_rate | 16000 | Частота дискретизации |
| n_fft | 800 | Размер FFT |
| hop_length | 200 | Шаг окна |
| win_length | 800 | Длина окна |
| n_mels | 80 | Количество mel-каналов |
| f_min | 55 | Минимальная частота |
| f_max | 7600 | Максимальная частота |

### Video Config

| Параметр | Значение | Описание |
|----------|----------|----------|
| img_size | 96 | Размер изображения лица |
| fps | 25 | Кадров в секунду |
| mel_step_size | 16 | Размер mel-чанка |
| mel_idx_multiplier | 80/fps | Множитель для синхронизации |

## 🔧 Технологический стек

### Основные библиотеки

| Библиотека | Версия | Назначение |
|------------|--------|------------|
| PyTorch | 2.8.0+ | Глубокое обучение |
| torchaudio | 2.8.0+ | Обработка аудио |
| torchvision | 0.23.0+ | Обработка изображений |
| NumPy | 2.1.0+ | Численные вычисления |
| OpenCV | 4.10.0+ | Обработка видео |
| librosa | 0.10.0+ | Дополнительная аудио обработка |
| scipy | 1.14.0+ | Научные вычисления |

### CUDA/GPU

- CUDA 12.8
- cuDNN 9.10
- Поддержка RTX 30/40/50 серий

## 🚀 Оптимизации

### GPU Ускорение

1. **Все операции на GPU**
   - Загрузка аудио → GPU
   - Mel-спектрограмма → GPU
   - Инференс модели → GPU

2. **Батч обработка**
   - Face detection батчи
   - Wav2Lip батчи
   - Параллельная обработка

3. **Память**
   - Эффективное использование VRAM
   - Автоматическое уменьшение батча при OOM
   - Освобождение памяти после детекции

### Производительность

- **RTX 5090**: ~200 FPS обработки
- **RTX 4090**: ~150 FPS обработки
- **RTX 3090**: ~100 FPS обработки

## 🎯 Дизайн-решения

### 1. Модульность
Каждый компонент независим и может быть заменен

### 2. Совместимость
Поддержка старых весов при новых библиотеках

### 3. Расширяемость
Легко добавить новые детекторы/процессоры

### 4. Документированность
Подробные комментарии и docstrings

### 5. Тестируемость
Модульные тесты для всех компонентов

---

**Modern Lipsync** - Современная архитектура для lip-sync 🏗️
