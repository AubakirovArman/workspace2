# 🎭 Веб-приложение "Говорящий Аватар"

Веб-сервис для создания видео с говорящим аватаром через TTS и Wav2Lip технологию синхронизации губ.

## 🚀 Возможности

- ✅ Предзагруженные модели в памяти (быстрая обработка)
- ✅ Автоматическая генерация TTS через API (русский, казахский, английский)
- ✅ Синхронизация губ с аудио через Wav2Lip
- ✅ Веб-интерфейс для удобной работы
- ✅ Поддержка GPU для ускорения

## 📋 Требования

- Python 3.8+
- CUDA (опционально, для GPU)
- ffmpeg
- Wav2Lip модель (`Wav2Lip-SD-GAN.pt` или `Wav2Lip-SD-NOGAN.pt`)
- Изображение аватара (`avatar.jpg`)

## 🔧 Установка

### 1. Установка системных зависимостей

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y ffmpeg

# macOS
brew install ffmpeg
```

### 2. Установка Python зависимостей

```bash
pip install -r requirements_web.txt
```

### 3. Подготовка файлов и переменных окружения

1. **Модели и ассеты**
   - GAN-модель: `/workspace/workspace2/Wav2Lip-SD-GAN.pt`
   - NoGAN-модель (опционально): `/workspace/workspace2/Wav2Lip-SD-NOGAN.pt`
   - Аватар (фото или видео): `/workspace/workspace2/avatar.jpg` или `IMG_3584.MOV`

2. **Файл `.env`**

   В проекте уже есть шаблон `.env`. Проверьте значения и измените при необходимости:

   ```bash
   AVATAR_PATH=/workspace/workspace2/avatar.jpg  # путь к фото или видео аватара
   AVATAR_STATIC_MODE=1                          # 1 — использовать статичное лицо, 0 — воспроизводить всё видео (для видео-аватара)
   AVATAR_FPS=25.0                               # FPS исходного материала (важно для видеорежима)
   
   GAN_CHECKPOINT_PATH=/workspace/workspace2/Wav2Lip-SD-GAN.pt
   NOGAN_CHECKPOINT_PATH=/workspace/workspace2/Wav2Lip-SD-NOGAN.pt
   
   ENABLE_SEGMENTATION=0                         # 1 включает face-parsing (требует моделей Wav2Lip-HD)
   ENABLE_SUPER_RESOLUTION=0                     # 1 включает ESRGAN (требует весов)
   ENABLE_REALESRGAN=0                           # 1 включает Real-ESRGAN (для HD-режима)
   REALESRGAN_OUTSCALE=4.0                       # масштаб Real-ESRGAN
   
   TTS_API_URL=https://tts.sk-ai.kz/api/tts      # внешний TTS
   
   LIPSYNC_HOST=0.0.0.0
   LIPSYNC_PORT=3000
   LIPSYNC_DEBUG=0
   ```

   > ⚠️ Если укажете видео в `AVATAR_PATH`, выставьте `AVATAR_STATIC_MODE=0`, чтобы задействовать полный ролик. Для фото или при желании ускорить обработку оставьте `AVATAR_STATIC_MODE=1`.

3. **Загрузка переменных окружения**

   ```bash
   cd workspace2
   export $(grep -v '^#' .env | xargs)
   ```

## 🎬 Запуск

```bash
cd workspace2
export $(grep -v '^#' .env | xargs)  # или используйте любую оболочку, которая читает .env
python app.py
```

Сервис запустится на `http://localhost:3000`.

> 💡 Для запуска без GPU обнулите `CUDA_VISIBLE_DEVICES` (например, добавьте в `.env` строку `CUDA_VISIBLE_DEVICES=`) или установите переменную перед стартом.

## 🌐 Использование

### Веб-интерфейс

1. Откройте браузер: `http://localhost:3000`
2. Введите текст для озвучки
3. Выберите язык (🇷🇺 Русский / 🇰🇿 Казахский / 🇬🇧 Английский)
4. Нажмите "🎤 Озвучить и создать видео"
5. Дождитесь обработки (обычно 15-30 секунд)
6. Смотрите результат прямо в браузере или скачайте видео

### API

#### POST `/api/generate`

Генерация видео с говорящим аватаром

**Запрос:**
```json
{
  "text": "Привет! Это тестовое сообщение.",
  "language": "ru",
  "static_mode": true
}
```

**Параметры:**
- `text` (обязательный) - текст для озвучки
- `language` (опционально) - язык: `ru`, `kk`, `en` (по умолчанию `ru`)
- `static_mode` (опционально) - `true` для статичного режима, `false` для видеорежима (если аватар — видео)

**Ответ:**
- Видео файл (MP4)

**Пример с curl:**
```bash
curl -X POST http://localhost:3000/api/generate \
  -H "Content-Type: application/json" \
  -d '{"text": "Привет мир", "language": "ru", "static_mode": false}' \
  --output result.mp4
```

**Пример с Python:**
```python
import requests

response = requests.post(
    'http://localhost:3000/api/generate',
    json={
        'text': 'Привет! Как дела?',
        'language': 'ru'
    }
)

with open('avatar_video.mp4', 'wb') as f:
    f.write(response.content)
```

#### GET `/api/health`

Проверка состояния сервиса

**Ответ:**
```json
{
  "status": "ready",
  "models_loaded": true,
  "avatar_loaded": true,
  "device": "cuda"
}
```

#### GET `/api/avatar`

Получить изображение аватара

## ⚡ Производительность

### Время обработки (примерно)

| Этап | CPU | GPU (CUDA) |
|------|-----|------------|
| Загрузка моделей (один раз) | ~10s | ~5s |
| TTS генерация | ~2-5s | ~2-5s |
| Lip-sync (10 сек аудио) | ~30-60s | ~5-10s |
| **Итого на запрос** | ~35-70s | ~10-20s |

**Оптимизация:**
- Модели загружаются один раз при старте
- Аватар держится в памяти
- Используйте GPU для 3-5x ускорения
- Статичное изображение обрабатывается быстрее видео

## 🗂️ Структура проекта

```
/workspace/
├── app.py                      # Главный Flask сервер
├── templates/
│   └── index.html              # Веб-интерфейс
├── modern-lipsync/             # Модули Wav2Lip
│   ├── service.py
│   ├── models/
│   ├── face_detection/
│   └── utils/
├── avatar.jpg                  # Изображение аватара
├── Wav2Lip-SD-GAN.pt          # Модель Wav2Lip
├── outputs/                    # Сгенерированные видео
├── temp_web/                   # Временные файлы
└── requirements_web.txt        # Python зависимости
```

## 🐛 Решение проблем

### "Face not detected"
- Убедитесь, что на изображении аватара четко видно лицо
- Попробуйте другое изображение с более фронтальным ракурсом
- Используйте изображение с хорошим освещением

### "CUDA out of memory"
- Уменьшите batch_size в `app.py`
- Используйте меньшее изображение аватара
- Переключитесь на CPU (медленнее, но работает)

### Медленная обработка
- Используйте GPU (в 3-5 раз быстрее)
- Убедитесь, что PyTorch установлен с CUDA поддержкой
- Используйте `Wav2Lip-SD-NOGAN.pt` (быстрее, но ниже качество)

### TTS API недоступен
- Проверьте интернет-соединение
- API может быть временно недоступен
- Попробуйте другой язык озвучки

## 🔒 Безопасность

При развертывании в production:

1. Добавьте аутентификацию
2. Ограничьте размер текста и частоту запросов
3. Используйте HTTPS
4. Настройте CORS правильно
5. Регулярно очищайте старые файлы

## 📝 Лицензия

Основано на проектах:
- [Wav2Lip](https://github.com/Rudrabha/Wav2Lip) - KR Prajwal et al.
- Modern Lipsync - оптимизированная версия

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все файлы на месте
3. Проверьте версии зависимостей
4. Попробуйте на CPU, если GPU не работает

---

**Приятного использования! 🎉**
