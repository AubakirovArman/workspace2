# H200 Оптимизация — краткая шпаргалка

## Архитектура под H200

```
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│   NVDEC     │───▶│  GPU (H200)  │───▶│  libx264    │
│  (decode)   │    │  lip-sync    │    │  (encode)   │
│  7x units   │    │  inference   │    │  CPU        │
└─────────────┘    └──────────────┘    └─────────────┘
```

### Почему так?

- ✅ **NVDEC** — H200 имеет 7 аппаратных декодеров (быстро)
- ✅ **GPU** — мощный чип для инференса (GAN/lip-sync)
- ✅ **libx264** — H200 НЕ имеет NVENC, используем CPU (надежно)

---

## Параметры кодирования

### libx264 preset (скорость vs качество)

| Preset | Скорость | Качество | Рекомендация |
|--------|----------|----------|--------------|
| `ultrafast` | ⚡⚡⚡ | ⭐⭐ | Максимум FPS |
| `veryfast` | ⚡⚡ | ⭐⭐⭐ | **Рекомендуется** |
| `fast` | ⚡ | ⭐⭐⭐⭐ | Баланс |
| `medium` | 🐌 | ⭐⭐⭐⭐⭐ | Максимум качества |

### CRF (качество)

| CRF | Качество | Размер файла | Рекомендация |
|-----|----------|--------------|--------------|
| 18 | Отличное | Большой | Для архива |
| **20** | Очень хорошее | Средний | **Оптимально** |
| 23 | Хорошее | Маленький | Для превью |
| 28 | Приемлемое | Очень маленький | Черновик |

---

## GOP параметры (автоматически настроены)

Для стабильной CFR и отсутствия «швов»:

```
keyint=2*FPS        # Ключевой кадр каждые 2 секунды
min-keyint=2*FPS    # Минимальный GOP = максимальному
scenecut=0          # Отключаем авто-вставку I-кадров
force-cfr=1         # Строгий CFR (без VFR)
```

**Зачем?**
- Идеальная синхронизация аудио/видео
- Нет «микродёрганий» при склейке
- Предсказуемый битрейт

---

## Быстрый старт

### Минимальный запрос (defaults)

```bash
curl -X POST http://localhost:5000/api/lipsync/video \
  -H "Content-Type: application/json" \
  -d '{"text": "Привет!", "language": "ru"}' \
  -o result.mp4
```

### Максимальная скорость

```bash
curl -X POST http://localhost:5000/api/lipsync/video \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Быстрая обработка",
    "language": "ru",
    "use_nvdec": true,
    "preset": "ultrafast",
    "crf": 23
  }' \
  -o fast.mp4
```

### Максимальное качество

```bash
curl -X POST http://localhost:5000/api/lipsync/video \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Лучшее качество",
    "language": "ru",
    "use_nvdec": true,
    "preset": "medium",
    "crf": 18
  }' \
  -o quality.mp4
```

---

## Проверка поддержки NVDEC

```bash
# Проверяем аппаратное ускорение
ffmpeg -hwaccels

# Должно быть в списке:
# cuda
```

Если `cuda` есть — можно использовать `use_nvdec: true`.

---

## Типичные узкие места

1. **TTS генерация** — ~2-3s (зависит от длины текста)
2. **Загрузка видео** — ~2s (для 119MB файла)
3. **Детекция лиц** — ~3-5s (зависит от кол-ва кадров)
4. **Инференс GAN** — ~10-20s (основное время, GPU)
5. **Кодирование** — ~5-10s (libx264 на CPU)

**Итого:** ~20-40s для 5-секундного видео.

---

## Оптимизация каждого этапа

### 1. TTS (вне контроля)
- Используйте короткие тексты для тестов
- Кэшируйте аудио если текст не меняется

### 2. Загрузка видео (можно ускорить)
- Используйте SSD для хранения IMG_3899.MOV
- Кэшируйте кадры в памяти (уже реализовано)

### 3. Детекция лиц (можно кэшировать)
- Детекция не зависит от аудио
- Можно сохранить координаты один раз и переиспользовать

### 4. Инференс (основное узкое место)
- Убедитесь что модель на GPU: `service.device == 'cuda'`
- Увеличьте `wav2lip_batch_size` в конфиге (default: 128)
- Используйте FP16 если модель поддерживает: `model.half()`

### 5. Кодирование (можно ускорить)
- Используйте `ultrafast` для черновиков
- `veryfast` — хороший баланс
- Для финальной версии — `medium`

---

## Конфиг в .env (пример)

```bash
# GPU
CUDA_VISIBLE_DEVICES=0
GPU_ID=0

# Батч-размеры (больше = быстрее, но больше VRAM)
FACE_DET_BATCH_SIZE=16
WAV2LIP_BATCH_SIZE=128

# Видео-база
BASE_VIDEO=/home/arman/musetalk/avatar/IMG_3899.MOV

# Кодирование (defaults)
VIDEO_ENCODER=libx264
VIDEO_PRESET=veryfast
VIDEO_CRF=20
USE_NVDEC=false
```

---

## Мониторинг производительности

### Во время обработки смотрите логи:

```
📊 Статистика:
   Инференс:           12.34s    ← основное время
   Обработано кадров:  150
   Скорость:           12.2 FPS  ← должно быть >10 FPS
```

**Если FPS < 10:**
- Проверьте `nvidia-smi` — GPU загружена?
- Увеличьте `wav2lip_batch_size`
- Используйте FP16 (если модель поддерживает)

**Если FPS > 20:**
- Отлично! Можно увеличить `crf` для лучшего качества
- Или использовать `fast`/`medium` preset

---

## Дополнительные настройки ffmpeg

### Если хочется ещё больше оптимизировать libx264:

```bash
# В video_lipsync_stream.py, функция start_video_encoder()
# Добавить параметры:

'-x264-params', 'keyint=50:min-keyint=50:scenecut=0:force-cfr=1:threads=auto:rc-lookahead=10'
```

**Параметры:**
- `threads=auto` — использовать все CPU-ядра
- `rc-lookahead=10` — лучшее распределение битрейта (чуть медленнее)

---

## Troubleshooting

### Видео тормозит при воспроизведении
- Проверьте GOP: должен быть `keyint=2*FPS`
- Убедитесь что `-movflags +faststart` установлен
- Попробуйте `crf=18` для лучшего качества

### Аудио не синхронизировано
- Проверьте `-shortest` флаг в энкодере
- Убедитесь что `-vsync 0` в декодере
- Проверьте `force-cfr=1` в x264-params

### Слишком медленно
1. `use_nvdec: true`
2. `preset: "ultrafast"`
3. `crf: 23`
4. Увеличьте `wav2lip_batch_size`

### Плохое качество
1. `preset: "medium"`
2. `crf: 18`
3. Проверьте исходное видео (должно быть HD)

---

## Полезные команды

```bash
# Проверить параметры видео
ffprobe -v error -show_streams IMG_3899.MOV | grep -E "width|height|r_frame_rate|codec_name"

# Проверить GPU
nvidia-smi

# Мониторинг в реальном времени
watch -n 1 nvidia-smi

# Проверить загрузку CPU
htop
```

---

## Итоговые рекомендации для H200

1. ✅ **NVDEC: включить** (`use_nvdec: true`)
2. ✅ **Preset: veryfast** (баланс)
3. ✅ **CRF: 20** (оптимальное качество)
4. ✅ **Батч-размер: 128** (уже в конфиге)
5. ✅ **FP16: опционально** (если модель поддерживает)

**Ожидаемая производительность:**
- 5-секундное видео: ~20-30s
- 10-секундное видео: ~35-50s
- 30-секундное видео: ~90-120s

**FPS инференса:**
- Минимум: 8-10 FPS
- Оптимально: 12-15 FPS
- Отлично: >20 FPS
