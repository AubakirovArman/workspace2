# 📘 API Usage Guide / Руководство по использованию API

## 🌐 Base URL
```
http://89.106.235.4:3000
```

---

## 🎯 Endpoints / Эндпоинты

### 1. 🏥 Health Check / Проверка состояния

**GET** `/api/health`

Проверяет готовность сервера и доступность моделей.

**Пример запроса:**
```bash
curl http://89.106.235.4:3000/api/health
```

**Ответ:**
```json
{
  "status": "ready",
  "parallel_gan_count": 4,
  "gan_model_loaded": true,
  "gan2_model_loaded": true,
  "gan3_model_loaded": true,
  "gan4_model_loaded": true,
  "avatar_mode": "static",
  "device": "cuda"
}
```

---

### 2. 🎬 Generate Avatar Video / Генерация видео (с автопараллелизмом)

**POST** `/api/generate`

Генерирует говорящий аватар из текста. Автоматически использует несколько GPU для длинных текстов (>10 секунд аудио).

**Request Body:**
```json
{
  "text": "Ваш текст для озвучки",
  "language": "ru",
  "static_mode": true
}
```

**Параметры:**
- `text` (обязательно) — текст для TTS и lip-sync
- `language` (опционально) — язык: `"ru"`, `"kk"`, `"en"` (по умолчанию `"ru"`)
- `static_mode` (опционально) — `true` для статичного лица, `false` для видео (по умолчанию `true`)

**Примеры:**

**Короткий текст (одиночный GPU):**
```bash
curl -X POST http://89.106.235.4:3000/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Привет! Как дела?",
    "language": "ru"
  }' \
  --output video.mp4
```

**Длинный текст (параллельно на 4 GPU):**
```bash
curl -X POST http://89.106.235.4:3000/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Добрый день! Сегодня мы обсудим важные вопросы технологического развития и искусственного интеллекта. Наша компания предлагает инновационные решения для автоматизации бизнес-процессов...",
    "language": "ru"
  }' \
  --output long_video.mp4
```

**Ответ:** MP4 файл (видео с аудио)

**Логи сервера:**
```
🧠 Параллельная генерация на 4 GPU: cuda:0, cuda:1, cuda:2, cuda:3
✅ Параллельная генерация завершена: 245 кадров, 4 чанков
```

---

### 3. 🔄 Stream Chunks / Потоковая генерация

**POST** `/api/stream_chunks`

Генерирует видео как единый клип (legacy endpoint, работает на одном GPU).

**Request Body:**
```json
{
  "text": "Текст для озвучки",
  "language": "ru",
  "static_mode": true
}
```

**Пример:**
```bash
curl -X POST http://89.106.235.4:3000/api/stream_chunks \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Могу предоставить информацию по интересующим вас вопросам",
    "language": "ru"
  }'
```

**Ответ:**
```json
{
  "success": true,
  "total_chunks": 1,
  "chunks": [
    {
      "index": 0,
      "text": "Могу предоставить информацию по интересующим вас вопросам",
      "video_url": "/api/chunk/video/clip_20251103_063541_123456",
      "audio_url": "/api/chunk/audio/clip_20251103_063541_123456",
      "duration": 4.12
    }
  ],
  "language": "ru",
  "mode": "static"
}
```

**Получение видео:**
```bash
curl http://89.106.235.4:3000/api/chunk/video/clip_20251103_063541_123456 \
  --output result.mp4
```

---

### 4. 📦 Segmented Generation / Сегментная генерация (полный контроль)

**POST** `/api/segment/generate`

Генерирует видео с разбиением на сегменты для параллельной обработки. Использует все доступные GPU.

**Request Body:**
```json
{
  "text": "Длинный текст для озвучки",
  "language": "ru",
  "segments": "auto",
  "batch_size": 4096
}
```

**Параметры:**
- `text` (обязательно) — текст
- `language` (опционально) — `"ru"`, `"kk"`, `"en"`
- `segments` (опционально) — количество сегментов или `"auto"` (по умолчанию `"auto"`)
- `batch_size` (опционально) — размер батча для инференса (по умолчанию `1024`)

**Пример:**
```bash
curl -X POST http://89.106.235.4:3000/api/segment/generate \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Добрый день! Это тест сегментированного кодирования с параллельной обработкой на нескольких видеокартах для максимальной производительности.",
    "language": "ru",
    "segments": "auto"
  }'
```

**Ответ:**
```json
{
  "success": true,
  "job_id": "segment_20251103_050605_789012",
  "video_url": "/api/segment/video/segment_20251103_050605_789012",
  "video_filename": "segment_20251103_050605_789012.mp4",
  "segments_used": 16,
  "total_frames": 245,
  "resolution": [512, 512],
  "timings": {
    "tts_time": 1.23,
    "audio_convert_time": 0.12,
    "capture_time": 2.89,
    "inference_time": 1.85,
    "encode_time": 0.67,
    "merge_time": 0.34,
    "audio_mux_time": 0.15,
    "total_time": 5.30
  },
  "capture_workers": 4,
  "capture_chunks": 4
}
```

**Получение видео:**
```bash
curl http://89.106.235.4:3000/api/segment/video/segment_20251103_050605_789012 \
  --output segmented.mp4
```

**Логи сервера:**
```
🧠 Сегментный пайплайн использует 4 GAN сервисов: cuda:0, cuda:1, cuda:2, cuda:3
✅ Параллельная генерация завершена
```

---

### 5. 🎥 WebRTC Streaming / Потоковое вещание

**POST** `/api/webrtc/start`

Запускает WebRTC сессию для real-time стриминга аватара.

**Request Body:**
```json
{
  "sdp": "v=0\r\no=- ...",
  "type": "offer",
  "text": "Текст для озвучки",
  "language": "ru",
  "low_latency": true
}
```

**Пример (из JavaScript):**
```javascript
const pc = new RTCPeerConnection({
  iceServers: [{ urls: 'stun:stun.l.google.com:19302' }]
});

const offer = await pc.createOffer();
await pc.setLocalDescription(offer);

const response = await fetch('http://89.106.235.4:3000/api/webrtc/start', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    sdp: offer.sdp,
    type: offer.type,
    text: 'Привет! Это WebRTC стрим',
    language: 'ru',
    low_latency: true
  })
});

const answer = await response.json();
await pc.setRemoteDescription(new RTCSessionDescription(answer));
```

---

## 🔑 Ключевые отличия эндпоинтов

| Endpoint | GPU | Скорость | Использование |
|----------|-----|----------|---------------|
| `/api/generate` | 1-4 (авто) | ⚡⚡⚡ | **Рекомендуется** для большинства задач |
| `/api/segment/generate` | 1-4 (всегда) | ⚡⚡⚡⚡ | Для очень длинных текстов с полным контролем |
| `/api/stream_chunks` | 1 | ⚡⚡ | Legacy, для обратной совместимости |
| `/api/webrtc/start` | 1 | ⚡ | Real-time стриминг |

---

## 🚀 Рекомендации по производительности

### Короткие тексты (<30 секунд аудио)
```bash
# Используйте /api/generate — автоматически выберет оптимальный режим
curl -X POST http://89.106.235.4:3000/api/generate \
  -H "Content-Type: application/json" \
  -d '{"text": "Короткая фраза", "language": "ru"}' \
  --output video.mp4
```

### Длинные тексты (>30 секунд)
```bash
# /api/generate автоматически распараллелит на все GPU
curl -X POST http://89.106.235.4:3000/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Очень длинный текст с большим количеством предложений...",
    "language": "ru"
  }' \
  --output long_video.mp4
```

### Максимальная производительность
```bash
# Используйте /api/segment/generate с явным контролем
curl -X POST http://89.106.235.4:3000/api/segment/generate \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Сверхдлинный текст...",
    "language": "ru",
    "batch_size": 4096
  }'
```

---

## 🌍 Поддерживаемые языки

- `ru` — Русский
- `kk` — Казахский
- `en` — Английский

---

## ⚙️ Конфигурация GPU

Текущая настройка: **4 GPU** (H200)

Для изменения количества GPU:
```bash
# .env файл
GAN_MAX_MODELS=4              # 1-8 GPU
CUDA_VISIBLE_DEVICES=0,1,2,3  # Список GPU
```

---

## 📊 Мониторинг

Проверка загрузки GPU:
```bash
nvidia-smi
```

Проверка статуса сервиса:
```bash
curl http://89.106.235.4:3000/api/health | jq
```

---

## 🐛 Отладка

**Проблема:** Видео не генерируется

**Решение:**
1. Проверьте `/api/health` — статус должен быть `"ready"`
2. Убедитесь что `parallel_gan_count > 0`
3. Проверьте логи сервера

**Проблема:** Медленная генерация

**Решение:**
1. Используйте `/api/generate` вместо `/api/stream_chunks`
2. Увеличьте `batch_size` в `/api/segment/generate`
3. Убедитесь что все GPU доступны (проверьте `CUDA_VISIBLE_DEVICES`)

---

## 📞 Примеры интеграции

### Python
```python
import requests

response = requests.post(
    'http://89.106.235.4:3000/api/generate',
    json={
        'text': 'Привет! Как дела?',
        'language': 'ru'
    }
)

with open('avatar.mp4', 'wb') as f:
    f.write(response.content)
```

### JavaScript (fetch)
```javascript
const response = await fetch('http://89.106.235.4:3000/api/generate', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    text: 'Привет! Как дела?',
    language: 'ru'
  })
});

const blob = await response.blob();
const url = URL.createObjectURL(blob);
// Используйте url для <video> элемента
```

### cURL
```bash
curl -X POST http://89.106.235.4:3000/api/generate \
  -H "Content-Type: application/json" \
  -d '{"text": "Привет!", "language": "ru"}' \
  --output video.mp4
```

---

## 📝 Notes / Примечания

- Все эндпоинты поддерживают CORS
- Максимальная длина текста не ограничена (лимит памяти GPU)
- Рекомендуется использовать `/api/generate` для большинства задач
- WebRTC работает только в современных браузерах
