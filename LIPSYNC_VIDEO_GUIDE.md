# Lip-sync на видео IMG_3899.MOV — руководство

## ✅ Что реализовано

Добавлен новый endpoint `/api/lipsync/video` для **потоковой** lip-sync обработки фиксированного видео **IMG_3899.MOV**.

### Архитектура (оптимизирована под H200)

```
Text → TTS → Audio → ffmpeg (NVDEC decode) → GPU lip-sync → ffmpeg (libx264 encode) → Video
```

**Преимущества:**
- ✅ **Без загрузки файлов** — используется фиксированное видео IMG_3899.MOV
- ✅ **Один проход** — нет промежуточных файлов и склейки
- ✅ **NVDEC опционально** — H200 имеет 7x NVDEC для аппаратного декодирования
- ✅ **libx264 (CPU)** — надежное кодирование с оптимальными параметрами CFR
- ✅ **Аудио сохраняется** — автоматическая синхронизация с видео

---

## 📦 Добавленные файлы

1. **`app_core/services/video_lipsync_stream.py`**
   - `process_video_lipsync_streaming()` — основная функция потоковой обработки
   - `start_video_decoder()` — запускает ffmpeg декодер (с опциональным NVDEC)
   - `start_video_encoder()` — запускает ffmpeg энкодер (libx264 с CFR параметрами)

2. **`app_core/services/utils_video.py`** (обновлен)
   - Добавлена поддержка NVDEC для декодирования
   - Оптимизированы параметры libx264: `-x264-params keyint=N:min-keyint=N:scenecut=0:force-cfr=1`
   - Аудио: AAC 192kbps @ 48kHz для совместимости

3. **`app_core/routes/api.py`** (обновлен)
   - Новый маршрут: `GET/POST /api/lipsync/video`
   - Упрощенный workflow: text → TTS → lip-sync на IMG_3899.MOV

4. **`templates/lipsync_video.html`**
   - Простая форма для ввода текста и выбора языка
   - Автоматическое скачивание результата

---

## 🚀 Как использовать

### 1. Проверьте наличие видео-базы

```bash
ls -lh /home/arman/musetalk/avatar/IMG_3899.MOV
```

Должен быть файл ~119MB.

### 2. Запустите сервер

```bash
cd /home/arman/musetalk/avatar
python app.py
```

### 3. Откройте в браузере

```
http://localhost:5000/api/lipsync/video
```

**Или используйте прямой URL:**
```
http://<HOST>:<PORT>/api/lipsync/video
```

### 4. Введите текст и нажмите "Создать видео"

Форма позволяет:
- Ввести текст для озвучки
- Выбрать язык (ru/en/kk)
- Автоматически скачает готовое видео

---

## 📡 API endpoint

### `POST /api/lipsync/video`

**Запрос (JSON):**
```json
{
  "text": "Привет! Это тестовое видео с синхронизацией губ.",
  "language": "ru",
  "use_nvdec": false,
  "encoder": "libx264",
  "crf": 20,
  "preset": "veryfast"
}
```

**Параметры:**
- `text` (обязательно) — текст для TTS
- `language` (опционально, default: `ru`) — язык: `ru`, `en`, `kk`
- `use_nvdec` (опционально, default: `false`) — использовать NVDEC для декодирования
- `encoder` (опционально, default: `libx264`) — кодек видео
- `crf` (опционально, default: `20`) — качество (18-23 оптимально)
- `preset` (опционально, default: `veryfast`) — скорость: `ultrafast`, `veryfast`, `fast`, `medium`

**Ответ:**
- `200 OK` — возвращает MP4-видео (`video/mp4`)
- `400 Bad Request` — если не указан `text`
- `404 Not Found` — если видео IMG_3899.MOV не найдено
- `503 Service Unavailable` — если модель не загружена

**Пример с curl:**
```bash
curl -X POST http://localhost:5000/api/lipsync/video \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Привет! Это тестовое видео.",
    "language": "ru",
    "use_nvdec": false,
    "encoder": "libx264",
    "crf": 20,
    "preset": "veryfast"
  }' \
  -o result.mp4
```

---

## ⚡ Оптимизация под H200

### 1. Включить NVDEC (опционально)

H200 имеет **7x NVDEC** — аппаратные декодеры для ускорения.

В запросе укажите:
```json
{
  "use_nvdec": true
}
```

**Проверка поддержки:**
```bash
ffmpeg -hwaccels | grep cuda
```

Должно вывести `cuda` в списке.

### 2. Настройка libx264 (CPU кодер)

H200 **не имеет NVENC**, поэтому используем libx264 на CPU.

**Рекомендуемые параметры:**

| Параметр | Значение | Описание |
|----------|----------|----------|
| `preset` | `veryfast` | Баланс скорость/качество (для максимальной скорости: `ultrafast`) |
| `crf` | `20` | Качество (18=отличное, 23=хорошее, меньше=лучше) |

**Для максимальной скорости:**
```json
{
  "encoder": "libx264",
  "preset": "ultrafast",
  "crf": 23
}
```

**Для максимального качества:**
```json
{
  "encoder": "libx264",
  "preset": "medium",
  "crf": 18
}
```

### 3. GOP и CFR параметры (уже настроены автоматически)

Для стабильной синхронизации используются:
- `keyint=2*FPS` — ключевой кадр каждые 2 секунды
- `min-keyint=2*FPS` — минимальный GOP тот же
- `scenecut=0` — отключаем автоматическую вставку ключевых кадров
- `force-cfr=1` — строгий CFR (Constant Frame Rate)

Это гарантирует отсутствие «швов» и идеальную синхронизацию аудио/видео.

---

## 📊 Статистика обработки

После завершения в логах сервера выводится:

```
📊 Статистика:
   TTS генерация:      2.35s
   Загрузка видео:     1.87s
   Детекция лиц:       3.42s
   Обработка аудио:    0.56s
   Инференс:           12.34s
   Обработано кадров:  150
   Скорость:           12.2 FPS
   Разрешение:         1080x1920
   Энкодер:            libx264
   NVDEC:              ✅
   ─────────────────────────
   ИТОГО:              20.54s
```

---

## 🔧 Устранение неполадок

### Ошибка: "Видео-база не найдена"

Проверьте путь:
```bash
ls /home/arman/musetalk/avatar/IMG_3899.MOV
```

Если файл отсутствует, укажите правильный путь в `app_core/routes/api.py`:
```python
BASE_VIDEO_PATH = '/путь/к/вашему/видео.MOV'
```

### Ошибка: "ffmpeg not found"

Установите ffmpeg:
```bash
sudo apt install ffmpeg
```

### Медленная обработка

1. **Включите NVDEC** (если H200 поддерживает):
   ```json
   {"use_nvdec": true}
   ```

2. **Используйте ultrafast preset**:
   ```json
   {"preset": "ultrafast", "crf": 23}
   ```

3. **Проверьте GPU-инференс**:
   - Убедитесь что модель загружена на GPU (`service.device == 'cuda'`)
   - Проверьте `CUDA_VISIBLE_DEVICES` в `.env`

### Проблемы с качеством видео

Увеличьте качество (за счет скорости):
```json
{
  "preset": "medium",
  "crf": 18
}
```

---

## 🎯 Следующие шаги (опционально)

1. **Добавить прогрессбар** — через WebSocket для отображения % обработки
2. **Кэширование детекции** — если видео не меняется, можно сохранить координаты лиц
3. **Батч-обработка** — для длинных видео (>5 мин) можно добавить чанки с overlap
4. **Параметры через UI** — настройка качества/скорости прямо в форме

---

## 📚 Дополнительные материалы

- **Параметры libx264**: https://trac.ffmpeg.org/wiki/Encode/H.264
- **NVDEC/NVENC**: https://developer.nvidia.com/video-codec-sdk
- **H200 спецификации**: https://www.nvidia.com/en-us/data-center/h200/

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера (stdout при запуске `app.py`)
2. Убедитесь что модель загружена: `/api/health` должен возвращать `status: ready`
3. Проверьте наличие видео IMG_3899.MOV
4. Убедитесь что ffmpeg доступен: `ffmpeg -version`

**Логи находятся в:**
- Stdout сервера (`python app.py`)
- Файлы в `outputs/` и `temp_web/`
